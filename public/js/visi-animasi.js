// card halaman utama

var animation = bodymovin.loadAnimation({
    container: document.getElementById('isi-krs-animasi'), // ID elemen HTML untuk animasi
    path: '../../isi-krs-animasi.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});
var animation = bodymovin.loadAnimation({
    container: document.getElementById('siakad-animasi'), // ID elemen HTML untuk animasi
    path: '../../siakad-animasi.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});
var animation = bodymovin.loadAnimation({
    container: document.getElementById('elearning-animasi'), // ID elemen HTML untuk animasi
    path: '../../elearning.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});
var animation = bodymovin.loadAnimation({
    container: document.getElementById('perpustakaan-animasi'), // ID elemen HTML untuk animasi
    path: '../../perpustakaan-animasi.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});

// animasi halaman fiskep

var animation = bodymovin.loadAnimation({
    container: document.getElementById('fiskep-visi-animasi'), // ID elemen HTML untuk animasi
    path: '../../fiskep-visi-animasi.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});
var animation = bodymovin.loadAnimation({
    container: document.getElementById('fiskep-misi-animasi'), // ID elemen HTML untuk animasi
    path: '../../fiskep-misi-animasi.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});
var animation = bodymovin.loadAnimation({
    container: document.getElementById('fiskep-tujuan-animasi'), // ID elemen HTML untuk animasi
    path: '../../fiskep-tujuan-animasi.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});

// animasi halaman hubungan internasional

var animation = bodymovin.loadAnimation({
    container: document.getElementById('hi-visi-animasi'), // ID elemen HTML untuk animasi
    path: '../../hi-visi-animasi.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});
var animation = bodymovin.loadAnimation({
    container: document.getElementById('hi-misi-animasi'), // ID elemen HTML untuk animasi
    path: '../../hi-misi-animasi.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});
var animation = bodymovin.loadAnimation({
    container: document.getElementById('hi-tujuan-animasi'), // ID elemen HTML untuk animasi
    path: '../../hi-tujuan-animasi.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});

// animasi halaman ilkom

var animation = bodymovin.loadAnimation({
    container: document.getElementById('ilkom-visi-animasi'), // ID elemen HTML untuk animasi
    path: '../../ilkom-visi-animasi.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});
var animation = bodymovin.loadAnimation({
    container: document.getElementById('ilkom-misi-animasi'), // ID elemen HTML untuk animasi
    path: '../../ilkom-misi-animasi.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});
var animation = bodymovin.loadAnimation({
    container: document.getElementById('ilkom-tujuan-animasi'), // ID elemen HTML untuk animasi
    path: '../../ilkom-tujuan-animasi.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});

// animasi halaman paud

var animation = bodymovin.loadAnimation({
    container: document.getElementById('paud-visi-animasi'), // ID elemen HTML untuk animasi
    path: '../../paud-visi-animasi.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});
var animation = bodymovin.loadAnimation({
    container: document.getElementById('paud-misi-animasi'), // ID elemen HTML untuk animasi
    path: '../../paud-misi-animasi.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});
var animation = bodymovin.loadAnimation({
    container: document.getElementById('paud-tujuan-animasi'), // ID elemen HTML untuk animasi
    path: '../../paud-tujuan-animasi.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});

// animasi halaman footer

var animation = bodymovin.loadAnimation({
    container: document.getElementById('map-pin'), // ID elemen HTML untuk animasi
    path: '../../map-pin.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});
var animation = bodymovin.loadAnimation({
    container: document.getElementById('whatsapp'), // ID elemen HTML untuk animasi
    path: '../../whatsapp.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});
var animation = bodymovin.loadAnimation({
    container: document.getElementById('email'), // ID elemen HTML untuk animasi
    path: '../../email.json', // Jalur ke file animasi JSON
    renderer: 'svg', // Format rendering (svg, canvas, html)
    loop: true, // Apakah animasi loop
    autoplay: true, // Apakah animasi otomatis diputar
});